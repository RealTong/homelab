apiVersion: v1
kind: Service
metadata:
  name: nine-router
  namespace: homelab
spec:
  selector:
    app: nine-router
  ports:
    - name: http
      port: 20128
      targetPort: http
      protocol: TCP
  type: ClusterIP
