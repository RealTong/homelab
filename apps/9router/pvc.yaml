apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nine-router-data
  namespace: homelab
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
