apiVersion: v1
kind: Service
metadata:
    name: kite
    namespace: observability
    labels:
        app: kite
spec:
    ports:
        - name: http
          port: 80
          protocol: TCP
          targetPort: 8080
    selector:
        app: kite
