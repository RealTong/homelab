apiVersion: v1
kind: Service
metadata:
  name: sub2api-postgres
  namespace: homelab
spec:
  selector:
    app: sub2api-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP
