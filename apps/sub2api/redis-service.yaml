apiVersion: v1
kind: Service
metadata:
  name: sub2api-redis
  namespace: homelab
spec:
  selector:
    app: sub2api-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
  type: ClusterIP
