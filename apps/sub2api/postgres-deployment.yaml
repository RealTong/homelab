apiVersion: apps/v1
kind: Deployment
metadata:
  name: sub2api-postgres
  namespace: homelab
  annotations:
    glance/icon: di:postgres
    glance/name: "Sub2API PostgreSQL"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sub2api-postgres
  template:
    metadata:
      labels:
        app: sub2api-postgres
    spec:
      nodeSelector:
        kubernetes.io/hostname: "realtong-server"
      containers:
        - name: postgres
          image: postgres:18-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data
            - name: POSTGRES_DB
              value: sub2api
            - name: POSTGRES_USER
              value: sub2api
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: sub2api-secret
                  key: POSTGRES_PASSWORD
            - name: TZ
              value: Asia/Shanghai
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - sub2api
                - -d
                - sub2api
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 5432
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 2Gi
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: sub2api-postgres-pvc
