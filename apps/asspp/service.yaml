apiVersion: v1
kind: Service
metadata:
  name: asspp
  namespace: homelab
  labels:
    app: asspp
spec:
  selector:
    app: asspp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
