apiVersion: v1
kind: Service
metadata:
  name: multica-web
  namespace: homelab
spec:
  selector:
    app: multica-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
