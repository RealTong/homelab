apiVersion: v1
kind: Service
metadata:
  name: multica-backend
  namespace: homelab
spec:
  selector:
    app: multica-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
