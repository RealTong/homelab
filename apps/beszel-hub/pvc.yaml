apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: beszel-hub-local-pvc
  namespace: observability
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
  storageClassName: local-path
