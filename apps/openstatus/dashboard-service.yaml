apiVersion: v1
kind: Service
metadata:
  name: openstatus-dashboard
  namespace: observability
  labels:
    app: openstatus-dashboard
spec:
  selector:
    app: openstatus-dashboard
  ports:
    - name: http
      port: 3000
      targetPort: 3000
