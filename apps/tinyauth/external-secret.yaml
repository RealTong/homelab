apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tinyauth-secret
  namespace: auth
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: infisical-store
    kind: ClusterSecretStore
  data:
    - secretKey: SECRET
      remoteRef:
        key: /TinyAuth/SECRET
    - secretKey: TINYAUTH_OAUTH_PROVIDERS_POCKETID_CLIENTID
      remoteRef:
        key: /TinyAuth/GENERIC_CLIENT_ID
    - secretKey: TINYAUTH_OAUTH_PROVIDERS_POCKETID_CLIENTSECRET
      remoteRef:
        key: /TinyAuth/GENERIC_CLIENT_SECRET