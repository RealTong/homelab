apiVersion: v1
kind: Service
metadata:
  name: immich-postgres
  namespace: databases
spec:
  selector:
    app: immich-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
