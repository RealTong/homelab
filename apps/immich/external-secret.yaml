apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: immich-secret
  namespace: homelab
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: infisical-store
    kind: ClusterSecretStore
  target:
    name: immich-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        DB_URL: "postgresql://immich:{{ .POSTGRES_PASSWORD | urlquery }}@immich-postgres.databases.svc.cluster.local:5432/immich"
        REDIS_HOSTNAME: "redis.databases.svc.cluster.local"
        REDIS_PORT: "6379"
        REDIS_PASSWORD: "{{ .REDIS_PASSWORD }}"
        REDIS_DBINDEX: "15"
  data:
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: /immich/POSTGRES_PASSWORD
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: /Redis/REDIS_PASSWORD
